import React from "react";

const SearchBar = () => (
  <div search-bar>
    <input type="text" placeholder="Search employees..." style={{ width: "590px", borderRadius: "5px", height: "30px" }} />
  </div>
);

export default SearchBar;
